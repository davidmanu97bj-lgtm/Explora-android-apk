name: Build EXPLORA APK from ZIP

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repo
        uses: actions/checkout@v4

      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Configurar Android SDK
        uses: android-actions/setup-android@v3

      - name: Instalar unzip
        run: sudo apt-get update && sudo apt-get install -y unzip

      - name: Descomprimir proyecto Android
        run: |
          rm -rf extracted
          mkdir extracted
          unzip -o EXPLORA-Android-WebView-project.zip -d extracted
          echo "Contenido descomprimido:"
          find extracted -maxdepth 4 -type f | head -100

      - name: Buscar carpeta del proyecto
        run: |
          PROJECT_DIR=$(find extracted -name "settings.gradle" -exec dirname {} \; | head -1)
          echo "PROJECT_DIR=$PROJECT_DIR" >> $GITHUB_ENV
          echo "Proyecto encontrado en: $PROJECT_DIR"

      - name: Instalar Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Compilar APK debug
        run: |
          cd "$PROJECT_DIR"
          gradle assembleDebug

      - name: Subir APK generado
        uses: actions/upload-artifact@v4
        with:
          name: EXPLORA-apk
          path: ${{ env.PROJECT_DIR }}/app/build/outputs/apk/debug/app-debug.apk
